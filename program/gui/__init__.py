import sys
import gui as _gui

# REMOVED: program.gui compatibility shim deleted (cleared)

# The `program.gui` compatibility shim was removed during migration; import
# directly from `gui` (e.g., `gui.app`).

