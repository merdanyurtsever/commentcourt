import sys

import utils as _utils

# Expose submodules for backward compatibility
# REMOVED: program.utils compatibility shims deleted (cleared)

# The `program.utils` compatibility package was removed. Use `utils` or
# `database.db` directly.
